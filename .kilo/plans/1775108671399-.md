# Plan: Compare install.sh with deep.sh and grok.sh

## Goal
Compare the current install.sh script with the user-provided deep.sh and grok.sh scripts to identify differences, improvements, and lessons learned for LLM stack setup.

## Current Status
- install.sh: Fully corrected script for Ubuntu WSL2 llama.cpp + Hermes Agent setup (1473 lines)
- deep.sh: Retrieved (1573 lines) - similar to install.sh but with quoted heredoc + envsubst placeholders, uses ggerganov/llama.cpp repo, streamlined HF token handling
- grok.sh: Retrieved (1573 lines) - streamlined version with core setup only, simplified error handling, uses ggerganov/llama.cpp, focuses on essentials

## Steps
1. Obtain deep.sh and grok.sh content
   - Check if files exist in workspace or GitHub repo
   - If not, request user to provide content or upload files

2. Analyze each script's approach
   - Core logic and structure
   - Error handling and validation
   - Model selection and dependencies
   - Launch script generation
   - Service management

   Findings:
   - deep.sh: Uses quoted heredoc + envsubst with @@ placeholders for cleaner variable substitution, avoids runtime expansion issues. Uses ggerganov/llama.cpp (official maintainer). Streamlined HF token loading. More robust cmake flags and build error handling.
   - grok.sh: Minimalist version, removes optional components (OpenClaude, video gen). Uses same approach as deep.sh. Focuses on core functionality with simpler error handling.
   - install.sh: Direct heredoc expansion, more features (OpenClaude, video gen), comprehensive bashrc helpers. Uses ggml-org/llama.cpp fork.

3. Compare implementations
   - Identify unique features in each
   - Note differences in robustness, automation, and compatibility
   - Evaluate pros/cons of different approaches

4. Extract lessons
   - Adopt quoted heredoc + envsubst with @@ placeholders for safer variable substitution
   - Use ggerganov/llama.cpp for official repo stability
   - Simplify optional components to reduce maintenance burden
   - Streamline HF token handling with better fallbacks
   - Improve cmake robustness and error reporting
   - Best practice: Separate runtime vs build-time variables clearly

5. Present findings
   - Summary: deep.sh/grok.sh offer cleaner variable handling and repo choices, install.sh provides more features
   - Recommended: Adopt quoted heredoc + envsubst approach and ggerganov/llama.cpp repo for install.sh
   - Decision: Prioritize deep.sh approach for reliability, keep install.sh features for completeness

## Questions for User
- Can you provide the content of deep.sh and grok.sh, or confirm their location?
- What specific fixes or improvements do deep.sh and grok.sh contain?
- Are there particular aspects of the scripts you'd like emphasized in the comparison?

## Success Criteria
- Clear comparison completed
- Identified quoted heredoc + envsubst as superior approach
- Recommended ggerganov/llama.cpp repo adoption
- Insights: Balance feature completeness with reliability</content>
<parameter name="filePath">/workspace/8a2131b2-e10d-4ca4-b0be-3cc27d8e159c/sessions/agent_02977236-1744-439d-aec0-924d61ca2f70/.kilo/plans/1775108671399-.md