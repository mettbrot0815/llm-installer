# Plan: Complete Rewrite of install.sh

## Goal
Rewrite install.sh incorporating deep.sh recommendations: quoted heredoc + envsubst with @@ placeholders, ggerganov/llama.cpp repo, while preserving all functionality (OpenClaude, video gen, full bashrc, model settings unchanged). Ensure the rewritten script is fully working.

## Current Status
- Original install.sh: 1473 lines, direct heredoc, ggml-org/llama.cpp, comprehensive features
- Target: Adopt deep.sh approach (quoted heredoc + @@ placeholders + envsubst, ggerganov/llama.cpp) but keep all install.sh functionality

## Steps
1. Structure the new script
   - Keep all 10 main sections (banner, HF token, system update, hardware, model selection, HF download, llama build, Hermes setup, launch script, bashrc)
   - Maintain all features: OpenClaude, video gen, full systemd services, comprehensive bashrc helpers

2. Key Technical Changes
   - Heredoc: Use quoted <<'TEMPLATE' with @@ placeholders (e.g., @@GGUF@@, @@MODEL_NAME@@)
   - envsubst: Call with specific variables: envsubst '${GGUF_PATH} ${SEL_NAME} ...'
   - Repo: Change git clone to https://github.com/ggerganov/llama.cpp.git
   - Build: Keep fingerprinting, --target llama-server, fallback logic
   - Launch script: Use @@ placeholders in template, envsubst to fill

3. Preserve Functionality
   - All model settings, grades, selections unchanged
   - OpenClaude npm install with ~/.npm-global
   - Video gen with diffusers
   - Full bashrc with all aliases and functions
   - Systemd services for all components
   - Error handling, validations, fallbacks

4. Ensure Working
   - Test variable substitution doesn't break runtime
   - Verify envsubst only affects build-time variables
   - Check cmake and build work with ggerganov repo
   - Validate all paths and commands

## Questions for User
- Should I keep all optional components (OpenClaude, video gen) or simplify per grok.sh?
- Any specific sections to prioritize in the rewrite?
- Want to test the rewritten script before finalizing?

## Risks
- envsubst with @@ placeholders might have edge cases
- ggerganov repo may have different build requirements
- Preserving all functionality increases complexity

## Success Criteria
- Rewritten script passes all checks (bash -n, variable validation)
- All original functionality preserved
- Superior variable handling from deep.sh approach
- Ready for testing on Ubuntu WSL2</content>
<parameter name="filePath">/workspace/8a2131b2-e10d-4ca4-b0be-3cc27d8e159c/sessions/agent_02977236-1744-439d-aec0-924d61ca2f70/.kilo/plans/1775108671399-.md